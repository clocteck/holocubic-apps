# 2026-09-15 实机验证

设备：192.168.0.226，App：qqmusic。所有诊断均不包含登录凭据。

## 已验证

- 模块构建、18项Lua/宿主逻辑测试通过；无浏览器运行的网页DOM回归测试通过。
- 微信取码、扫码确认、音乐凭据交换、SD保存成功；重启恢复会话成功。
- QQ取码PNG和等待扫码轮询成功，QQ账号最终手机确认尚未单独验收。
- 启动默认进入收藏，游客只有热歌/新歌；登录后五栏目。
- 实际读取765首收藏、300首热歌、100首新歌，均完成后台预取。
- 热歌/新歌20个元数据页写入SD，重启后进入热歌榜命中SD（disk.hits=1）。
- 平台账户昵称、头像与超级会员状态读取成功，会员有效期已由平台返回。
- 实际播放：MP3，bitrate=128，source_rate=44100，队列765首。
- 播放前后列表均21项，列表revision保持不变。
- 同次播放测得internal_peak_delta=52752字节（大于50KiB），播放继续且无错误。
- 退出登录逻辑只做宿主测试/代码检查，未主动注销用户实机会话。

## 仍未打通 / 明确边界

- 每日推荐旧网页入口 HTTP 200，但未返回有效“今日私享”歌单；显示明确不可用提示，不伪造日推。
- 搜索接口游客实测返回空列表，登录后搜索仍需单独验收。
- 最近播放是当前账号本机记录，不是QQ云端历史。
- 未实现QQ音乐App MQTT扫码，遵照用户要求仅QQ/微信。
- 用户禁止未经许可的computer-use，之后验证仅采用源码、宿主测试和设备HTTP API。
