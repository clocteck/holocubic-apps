# 设备测试记录

设备：http://192.168.0.226/
安装路径：/sd/apps/netease

- 安装包逐文件上传成功，以上传响应 entry.size 验证文件大小，未回读文件校验。
- 模块加载、纯黑界面、本机二维码生成、网易云扫码轮询成功。
- 用户手机确认后，设备收到 HTTP 200 / 网易云 code=803。
- 登录 Cookie 不完整：has_music_u=false，has_csrf=false。
- 当前固件 push_header_pairs 对重复 Set-Cookie 使用 lua_setfield 覆盖；
  Lua net 明确拒绝 secure 连接，App 内无现成原始 TLS HTTP 替代。
- 已避免「验证登录」无限停留：空账号类型检查、回调异常提示、20 秒 API 超时；
  登录成功后先保存会话再请求账号信息（前提是固件完整提供 Cookie）。
- 等待扫码阶段采样：内部 RAM 峰值增量 10788 字节，压缩流缓冲 262144 字节；
  这不是完整播放/TLS 全时峰值验收结果。
- HTTP 诊断：http://192.168.0.226/netease/status（不含 Cookie、用户 ID 或音乐 URL）。
- 因缺失 Cookie，尚未完成账号歌单、播放、暂停、切歌和断电免登录实机验收。
- 本地 9 项测试通过，模块无未解析全局符号。PC 非登录在线探测获得普通 MP3 地址。
- 未修改其它 App 源码，未修改或烧录固件。

## 固件 1.211 修复后复测

随后用户明确授权修改、编译及烧录固件。基于 cubic/develop 最新提交
0042bc9fa6a47c71234f85010720029097c6d332，修复重复 Set-Cookie 转交逻辑。
版本 1.211，使用本机 ESP-IDF 5.5.4 构建；COM35、ESP32-S3，Flash 加密开启。
仅通过 --encrypt 写入 app0 的 0x10000 应用镜像，未改 bootloader、分区表、
NVS、eFuse 或 SD 数据。

设备返回 current_version=1.211、current_sha256=485af5ca8、
running_partition=app0、boot_partition=app0、boot_validated=true。

同一台局域网合成响应服务器返回三条 Set-Cookie：

| 固件 | 普通 http.get | 流式 connection headers |
| --- | --- | --- |
| 1.210 | HTTP 200，但缺失 Cookie | HTTP 200，但缺失 Cookie |
| 1.211 | 全部保留，通过 | 全部保留，通过 |

测试使用假凭据，并覆盖 Expires 中的逗号。正常 app.info 已恢复，
临时设备 header_probe.lua 已清理，测试结果 JSON 留在 App 目录供排查。
真实账号重新扫码和音乐播放仍需继续确认。

## 2026-09-15 UI 接续部署

- 在已登录的设备上上传新版 Lua、13/16px 字库、Logo 和重新构建的动态模块。
- 上传响应逐文件长度一致；未读取或覆盖 /sd/data/netease 凭据与设置内容。
- 重启后 alive=true、logged_in=true、ui_ready=true、ui_version=2，无登录错误。
- page=about 的控制请求成功，设备网页 HTTP 200 且含“回到主页”链接。
- 本地 14 项测试通过，包括五行歌词、说明页导航、分步字体加载及 UI 状态切换。
- 设备 PNG 截图导出返回 PNG encode failed；临时截图控制代码已撤除。
  尚未完成像素级实机截图验收，不以 HTML 设计稿代替实机截图。
- 本轮部署前设备有“平台未返回 MP3，已拒绝播放”旧错误；本轮未验证连续播放、
  听感或完整 TLS/播放内存峰值。

## 后续显示与增益修复

- 复现旧版静态歌单页连续 10 次 render 仍提交 560 次 LVGL 更新。
  新版对六个页面的相同状态重复渲染均为零写入；选择变化和进度变化仍正确更新。
- 覆盖每次换图后 pivot 重置模拟、方形/宽图定位和无变化封面的零更新测试。
- 新输出增益为旧部署的 2 倍，原生测试验证低幅比例、静音、正负饱和与削波计数。
- UI v4：设备本地 HH:MM（getlocal 已含 DST）替代 MP3 文案；采样率显示 44.1k 等。
- 16 项本地测试通过，包括时钟异常/DST 不重复加时、字库 ascent 校验和固定
  31px 歌词基线间距。以上为主机逻辑验证，未替代实机视觉或听感验收。

## UI v5：收藏 / 最近播放 / 每日推荐

- 已部署并恢复登录；收藏改为独立喜欢歌曲接口，最近播放改为账号播放记录接口。
- 18 项本地测试通过，覆盖大整数 ID 的 JSON 预处理、来源响应、顺序和去重。
- 实机依次验证：收藏 4 首（详情 code=200），最近播放 5 首（code=200），
  每日推荐首屏 20 首加下一页项（code=200）；三项 error 均为空。
- 最后返回第一项“收藏”的歌曲列表；登录凭据和设置未由部署覆盖。
- 最近播放取云端最近 50 条；本轮未实现额外的本机历史持久化或云端听歌上报。

## UI v6：封面、文字与缓冲

- 已部署：歌词固定基线间距 30px；两行 13px 歌名固定高 36px，与歌手区隔离；
  网页同步账号栏目说明、本地时间/采样率及封面网络错误重试。
- 音频网络块 6144 字节；环形缓冲 393216 字节；PCM 暂存 4096 字节；DMA 6×512。
- 20 项主机测试通过，覆盖缩略图失败重试/音频优先、长歌名区域、非二次幂环形绕回。
- 重启后未进入列表即预取收藏封面；随后最近播放、每日推荐、收藏栏目均在 library
  页面显示 covers.ready=true，三张封面均 136×136，HTTP 200，errors=0。
- 启动收藏歌曲后 5 次采样持续 playing，位置从约 2 秒推进至 14.3 秒；接收音频
  631503 字节，超过单个环形缓冲容量，未报错；缓冲约 387KiB，播放封面 ready=true。
- 实测该短时播放内部 heap 峰值增量 26136 字节，低于 50KiB 目标；不据此宣称
  已证明所有 TLS 瞬时峰值、长期稳定性或听感。
